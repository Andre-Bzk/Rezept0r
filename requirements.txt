# Pinned to the versions verified on the Pi. Bump deliberately, not implicitly:
# an unpinned rebuild months later resolves different versions than the running
# container and breaks in ways the diff cannot explain.
fastapi==0.139.2
uvicorn==0.51.0
pydantic==2.13.4
pydantic-settings==2.14.2
httpx==0.28.1
python-multipart==0.0.32
recipe-scrapers==15.11.0
beautifulsoup4==4.15.0
lxml==6.1.1
openai==2.46.0
python-dotenv==1.2.2
aiofiles==25.1.0
